/* import React from "react";

function ProductCard({ product }) {
  return (
    <div className="bg-white rounded-xl shadow-md p-3 flex flex-col">
      <img
        src={product.image}
        alt={product.name}
        className="w-full h-24 object-contain mb-2"
      />
      <p className="text-sm font-medium mb-1">{product.name}</p>
      <p className="text-xs text-gray-600 mb-2">${product.price}</p>
      <button className="bg-blue-600 text-white rounded-lg py-1 text-sm hover:bg-blue-700 transition">
        Agregar al carrito
      </button>
    </div>
  );
}

export default ProductCard;/*/

